<%= render 'books/list.index', book_new: @book_new %>
  <div class="col-8 offset-1 ">
   <div class="row">
    <h1 class="h2">Users</h1>
   </div>
    <div class="row">
     <table class="col table ">
      <tr class="border-top border-bottom mx-auto ">
      <th class="col-4 p-2 "> image</th>
      <th class="col-5 p-2 ">name</th>
      <th class="col p-2"></th>
     </tr>
      <% @users.each do |user| %>
       <tr class="border-bottom ">
        <td class=""><%= image_tag user.get_profile_image(80,80) %></td>
        <td class=""><%= user.name %></td>
        <td class=""><%= link_to "Show", user_path(user.id) %></td>
       </tr>
      <% end %>
      </table>
     </div>
  </div>
 </div>
</div>