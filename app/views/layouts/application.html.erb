<!DOCTYPE html>
<html>
  <head>
    <title>Bookers2</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body class="flex-column ">
    <header class="">
      <nav class="navbar navbar-expand-lg navbar-light bg-dark ">
        <div class="container ">

          <a class="navbar-brand text-white" href="/">Bookers</a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
                        aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown ">
            <ul class="navbar-nav ml-auto ">
            <% if user_signed_in? %>
            <li class="nav-item">
               <%= link_to user_path(current_user), class: 'nav-link text-white' do %>
                <i class="fas fa-home"></i> Home
                <% end %>
            </li>
            <li class="nav-item">
               <a href="/users" class="nav-link text-white"><i class="fas fa-users"></i> Users</a>
            </li>
            <li class="nav-item">
               <a href="/books" class="nav-link text-white"><i class="fas fa-book-open"></i> Books</a>
            </li>
            <li class="nav-item">
              <a href="/users/sign_out" class="nav-link text-white" data-method="delete">
                <i class="fas fa-sign-out-alt"></i> Log out</a>
            </li>
            <% else %>
             <li class="nav-item">
               <a href="/" class="nav-link text-white"><i class="fas fa-home"></i> Home</a>
            </li>
             <li class="nav-item">
               <a href="/homes/about" class="nav-link text-white"><i class="fas fa-link"></i> About</a>
            </li>
            <li class="nav-item">
              <a href="/users/sign_up" class="nav-link text-white"><i class="fas fa-user-plus"></i> Sign up</a>
            </li>
            <li class="nav-item">
              <a href="/users/sign_in" class="nav-link text-white"><i class="fas fa-sign-in-alt"></i> Log in</a>
            </li>
            <% end %>
            </ul>
          </div>
      </nav>
    </header>
    <% flash.each do |message_type, message| %>
    <%= content_tag :div, message, class: message_type %>
    <% end %>
    <%= yield %>
     <footer class="text-black pt-5 ">
      <p class="text-center">CopyRight Infratop.inc</p>
    </footer>
  </body>
</html>
